package com.oncall.domain.enums;

public enum HandoverParticipantRole {
    /** Member belongs to the outgoing (departing) on-call group. */
    OUTGOING,

    /** Member belongs to the incoming (taking over) on-call group. */
    INCOMING
}
